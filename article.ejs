<!DOCTYPE html>
<html lang="fa">
<head>
  <meta charset="UTF-8">
  <title><%= article.title %> - خبرگزاری من</title>
  <link rel="stylesheet" href="/public/css/style.css">
</head>
<body>
  <header>
    <h1><a href="/">خبرگزاری من</a></h1>
  </header>

  <main class="article-page">
    <article>
      <h2><%= article.title %></h2>
      <p class="meta"><%= article.author %> — <%= article.published_at ? new Date(article.published_at).toLocaleString() : '' %></p>
      <% if (article.featured_image) { %>
        <img src="<%= article.featured_image %>" alt="<%= article.title %>">
      <% } %>
      <div class="content"><%- article.body %></div>
    </article>
  </main>

  <footer>
    <p>© خبرگزاری من</p>
  </footer>
</body>
</html>